$( "#comment_<%= @comment.id %>" ).effect( "blind", { percent: 0 }, 500, callback );
function callback() {
  $( "#comment_<%= @comment.id %>" ).remove();
  $( "#comments_count_<%= @comment.post_id %>" ).text("(<%= @comment.post.comments.count %>)");
};