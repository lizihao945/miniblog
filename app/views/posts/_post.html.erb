<%= form_for [post, Comment.new],
html: { id: "new_comment_on_#{post.id}" },
remote: true do |f| %>
  <p>
	<%= f.label :content, "New Comment" %>
	<br />
	<%= f.text_area :content, :rows =>5 %></p>
  <p>
  <%= f.submit "Add Comment" %></p>
<% end %>